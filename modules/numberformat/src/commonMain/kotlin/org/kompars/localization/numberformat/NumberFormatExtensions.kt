package org.kompars.localization.numberformat

public fun Byte.format(format: NumberFormat): String {
    return format.format(toString())
}

public fun Short.format(format: NumberFormat): String {
    return format.format(toString())
}

public fun Int.format(format: NumberFormat): String {
    return format.format(toString())
}

public fun Long.format(format: NumberFormat): String {
    return format.format(toString())
}

public fun Float.format(format: NumberFormat): String {
    return format.format(toString())
}

public fun Double.format(format: NumberFormat): String {
    return format.format(toString())
}
